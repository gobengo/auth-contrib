var authContrib = require('auth-contrib');
var assert = require('chai').assert;

describe('auth-contrib', function () {
    it('is an object', function () {
        assert.instanceOf(authContrib, Object);
    });
});
